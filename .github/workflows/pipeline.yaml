name: Linting, Testing, Building and Deploying

on:
  pull_request:
    branches:
      - main
      - development
  push:
    branches:
      - main

  workflow_dispatch:

env:
    GITHUB_BASE_REF: ${{ github.base_ref }}
    GITHUB_HEAD_REF: ${{ github.head_ref }}
    GITHUB_EVENT_NAME: ${{ github.event_name }}

jobs:
  Linting:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v3
        with:
            fetch-depth: 0

      - name: Load & Cache Dependencies
        uses: ./.github/actions/cached-deps

      - name: Get Nx Shas
        uses: nrwl/nx-set-shas@v3

      - name: Run Affected Lint in Pull Request Event
        if: env.GITHUB_EVENT_NAME == 'pull_request'
        run: npx nx affected:lint --base=origin/${{env.GITHUB_BASE_REF}} --head=origin/${{env.GITHUB_BASE_REF}} --parallel=3

      - name: Run All Lints in other Events
        if: env.GITHUB_EVENT_NAME != 'pull_request'
        run: |
            npx nx lint d-client
            npx nx lint d-server

  Testing:
    needs: Linting
    runs-on: ubuntu-latest

    steps:
      - name: Get Code
        uses: actions/checkout@v3
        with:
            fetch-depth: 0

      - name: Load & Cache Dependencies
        uses: ./.github/actions/cached-deps

      - name: Get Nx Shas
        uses: nrwl/nx-set-shas@v3

      - name: Run Affected Unit Tests in Pull Request Event
        if: env.GITHUB_EVENT_NAME == 'pull_request'
        run: npx nx affected:test --base=origin/${{env.GITHUB_BASE_REF}} --head=origin/${{env.GITHUB_BASE_REF}} --parallel=3

      - name: Run Affected Integration Tests in Pull Request Event
        if: env.GITHUB_EVENT_NAME == 'pull_request'
        run: npx nx affected:test:int --base=origin/${{env.GITHUB_BASE_REF}} --head=origin/${{env.GITHUB_BASE_REF}} --parallel=3 --exclude=d-client

      - name: Run All Unit Tests in other Events
        if: env.GITHUB_EVENT_NAME != 'pull_request'
        run: |
            npx nx test d-client
            npx nx test d-server

      - name: Run All Integration Tests in other Events
        if: env.GITHUB_EVENT_NAME != 'pull_request'
        run: npx nx test:int d-server

